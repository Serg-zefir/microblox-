h2.postTitle Write a new post
form(name='form', novalidate)
  div(ng-class='{error: form.title.$invalid && !form.title.$pristine}')
    label(for='title') Title:
    input(ng-model='post.title', name='title', required)
    div(ng-show='form.title.$error.required && !form.title.$pristine') Title is required
  div(ng-class='{error: form.body.$invalid && !form.body.$pristine}')
    p Content:
    textarea(ng-model='post.body', name='body', cols='50', rows='15', required)
    div(ng-show='form.body.$error.required && !form.body.$pristine') Content is required
    p
      button(ng-click='submitPost()', ng-disabled='form.$invalid') Submit Post
